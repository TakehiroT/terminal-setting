// Zellij 基本設定
// デフォルトレイアウトをIDEに設定
default_layout "ide"

// キーバインド設定
keybinds {
    shared {
        // Alt+1/2/3/4/5: タブ切り替え (Code/Git/Diff/Impl/Monitor)
        bind "Alt 1" { GoToTab 1; }
        bind "Alt 2" { GoToTab 2; }
        bind "Alt 3" { GoToTab 3; }
        bind "Alt 4" { GoToTab 4; }
        bind "Alt 5" { GoToTab 5; }

        // Alt+m: アクションメニュー (tmuxと同じ)
        bind "Alt m" {
            Run "bash" "-c" "~/.config/zellij/scripts/menu.sh" {
                floating true
                close_on_exit false
            }
        }
    }
}

// テーマ設定
theme "default"

// マウスサポート
mouse_mode true

// コピーモード設定
copy_on_select true

// セッション名をシリアライズ
session_serialization true

// ペインフレーム設定
pane_frames true

// スクロールバッファサイズ
scroll_buffer_size 10000

// UIの簡略化を無効 (ガイドを常に表示)
simplified_ui false

// キーバインドはZellijデフォルトを使用
// 画面下部のステータスバーにガイドが表示されます
//
// === 基本操作ガイド ===
//
// Ctrl+p  → ペインモード (Pane)
//           h/j/k/l: 移動, n: 新規(下), r: 新規(右), x: 閉じる, f: 全画面
//
// Ctrl+t  → タブモード (Tab)
//           h/l: 左右移動, n: 新規, x: 閉じる, r: リネーム, 1-9: 番号で移動
//
// Ctrl+n  → リサイズモード (Resize)
//           h/j/k/l: サイズ変更
//
// Ctrl+h  → 移動モード (Move)
//           h/j/k/l: ペインを移動
//
// Ctrl+s  → スクロールモード (Scroll)
//           j/k: スクロール, d/u: 半ページ, PageDown/Up: ページ
//
// Ctrl+o  → セッションモード (Session)
//           d: デタッチ, w: セッションマネージャー
//
// Ctrl+q  → Zellijを終了
//
// Alt+1/2/3/4/5 → タブ切り替え (カスタム)
//           1: Code, 2: Git, 3: Diff, 4: Impl, 5: Monitor
//
// Alt+m   → アクションメニュー (カスタム)
//           Activate Skills, Restart, Git, Cleanup
//
// Enter/Esc → ノーマルモードに戻る
